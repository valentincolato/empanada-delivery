# Copy this file to .env.local and fill in any secrets needed locally.
# .env.local is gitignored — never commit real credentials.

# Rails master key (required in production; optional in dev if config/master.key exists)
# RAILS_MASTER_KEY=

# Override mailer in development (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=secret
